const { ArchivosRoutes } = require("./archivos");
const {
  AllProyectos,
  CreateProyecto,
  GetProyectos,
  AgregarArchivoProyecto,
} = require("./proyectos");

module.exports = {
  ArchivosRoutes,
  AllProyectos,
  CreateProyecto,
  GetProyectos,
  AgregarArchivoProyecto,
};
