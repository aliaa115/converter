const { AllProyectos } = require("./allProyectos");
const { CreateProyecto } = require("./crearProyecto");
const { GetProyectos } = require("./getProyecto");
const { AgregarArchivoProyecto } = require("./editarProyecto");

module.exports = {
  AllProyectos,
  CreateProyecto,
  GetProyectos,
  AgregarArchivoProyecto,
};
